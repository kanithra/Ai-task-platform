# ⚠️  DO NOT commit real secrets. Use a secrets manager or Sealed Secrets.
# Generate base64: echo -n 'your-secret' | base64
# For production: use Bitnami Sealed Secrets or AWS Secrets Manager with External Secrets Operator

apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: ai-task-platform
type: Opaque
data:
  # Replace with actual base64-encoded values:
  # echo -n 'your-super-secure-jwt-secret-min-32-chars' | base64
  JWT_SECRET: eW91ci1zdXBlci1zZWN1cmUtand0LXNlY3JldC1taW4tMzItY2hhcnM=
  # echo -n 'your-redis-password' | base64
  REDIS_PASSWORD: ""
